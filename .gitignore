__pycache__/
*.py[cod]
.venv/
venv/
# Every env file, not just `.env` -- `.env.dev` carries an OpenAI key and
# `.env.prod` names production resources. The example is the only one tracked.
.env
.env.*
!.env.example
.dev/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
*.egg-info/
.DS_Store
